# Changelog

このプロジェクトの変更履歴。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [Unreleased]

### Fixed

- **閲覧履歴で、同じ本が複数の日にあると 2 つ目以降のカードをクリックしても開かない**: 履歴は
  「日 × 本」で 1 枚描くため、同じ本を別の日に読むと同じ本のカードが複数枚出る（意図どおりの
  挙動）。しかしカードの要素 id が `history-card-{本 id}` と**本 id だけで作られていた**ため、
  重複した本では要素 id が衝突していた。GPUI は要素 id が一意であることが前提で、要素の状態
  （クリック / ホバー / 選択）が id 単位で共有されるため、**2 つ目以降のカードはクリックしても
  ビューアーが開かず**、**選択枠も同じ本の全出現に同時に付いていた**（選択判定も
  `selected_id == item.book.id` と本 id で比較していた）。タグ列の「+n」展開も本 id で
  持っていたため、同じ本の別の日の 1 枚まで一緒に開いていた。
  出現キー `{日付}-{その日の連番}`（例 `2026-09-21-0`）を作り、カード / 行 / 表紙 / お気に入り
  ハート / ダウンロード済みバッジ / タグ行・タグトグル / 状態チップ / サークル・作者チップの
  **要素 id と debug_selector に前置**した（`history-card-2026-09-21-0-{本 id}`）。選択は
  `selected_occurrence()` で平坦位置を「何日目の何冊目か」に直して比較し、タグ展開も出現キーで
  持つようにした。日付バーやヘッダなど本 id を含まない id は変更していない（見た目・並び順も
  変えていない）。
  検証: 実データで「1 日だけに出る本（`m5st5`）は開く / 2 日（`陰キャボサ髪手汗女`）・3 日
  （`魔法少女（？）プラム`）に出る本はどちらの出現も無反応」を再現してから修正し、修正後は
  同じ本の両方の出現が開くこと、選択枠が 1 つの出現だけに付くことをアプリで確認。新規テスト
  3 本（`repeated_books_get_distinct_elements` /
  `selection_follows_the_occurrence_not_the_book` / `tag_expansion_is_per_occurrence`）、
  `cargo test -p thundoku-shelf --lib` 324 件全緑、clippy `-D warnings` / fmt クリーン。
- **ビューアーのページが網点だらけに見える（モアレ）**: ページ画像を**フル解像度のまま GPU に
  渡していた**ため、見開きなどの小さい表示では実質 1 タップのサンプリングで 6 倍前後の縮小が
  行われ、スクリーントーン（網点）がクロスハッチ状のモアレになって潰れていた
  （実測: 4441x6213 のページを 690px 幅で表示したときに発生。パックの画像自体は正常）。
  **デコード直後に表示に必要な幅へ高品質フィルタで前縮小**してから GPU に渡すようにした
  （`PageLoader::load_for_display` / `ImageViewer::update_display_target`。縮小は面積平均 →
  `Lanczos3` の 2 段）。モアレが消え、メモリも約 1/6（約 110MB/ページ → 約 16MB）になる。
  ズームやウィンドウ拡大で必要幅が増えたページだけ読み直す。

## [0.2.5] - 2026-09-20

初めて使う人向けの案内を説明画面に足し、**サイトのログイン直後にそのサイトを自動で同期**する
ようにした（初回ログインで本棚が空のままにならない）。サイドバーは選択中の項目が塗りつぶしで
分かるようになり、どのサイトにもログインしていなければ起動時に説明画面を開く。閲覧履歴の
見出しから件数表示は外した。

### Added

- **サイトのログイン直後に、そのサイトを自動で同期**: 初回ログインのあと本棚が空のままだと
  次に何をすればよいか分からないため、ログインの完了時にそのサイトの同期を 1 回だけ実行する
  （手動の「同期」ボタンはそのまま使える。同期の実体は共通の `sync_site`）。
  「初回」に限らずログイン操作のたびに走るが、**セッションの復元（再起動）では発火しない**ので
  起動のたびに同期が走ることはない。Google / GitHub のログインでは同期しない（本の供給元では
  ないため）。説明画面の「はじめての方へ」「本棚の基本的な使い方」も自動同期に合わせて更新した。
- **説明画面に「はじめての方へ」「サイドバーの使い方」「本棚の基本的な使い方」を追加**:
  初回に何をすればよいかが説明画面に無かったため、「このアプリについて」の次に
  「ストアにログイン → 本棚に取り込む → 読む」の 3 ステップを置いた（初回の案内は短く保つ方針で、
  詳細は下の節に任せる）。あわせてサイドバーの項目一覧（実際の行と同じ順）と
  **アカウントからのログイン方法**（Google / GitHub / ストアで認証方法が違うこと）、
  本棚の基本操作（同期 / 読む / 絞り込み / 並び替え / 表示切替 / マウスとキーボード）を追記した。
  「ログインしなくてもローカルの本だけでも読める」「残りは『同期』でまとめて取り込める」の
  ような、実装に無い使い方を想像させる書き方は入れない。

### Changed

- **サイドバーで選択中の項目が分かるようにした**: 選択中の行を**塗りつぶし**（下地 = `primary`、
  アイコン / ラベル = `primary_foreground`、ラベルは太字）にした。以前は選択の印が下地
  （`secondary`）だけで、**アイコンだけの幅ではどれが選択中か分からなかった**（ホバーの下地と
  同じ色だったため余計に紛らわしかった）。非選択のアイコン / ラベルは `muted_foreground` に
  落とし、ホバーの下地は `muted` にして選択と区別する。サイト別サブメニュー（すべての本 /
  技術書典 / BOOTH / FANZA同人 / DLsite）も選択中は文字を `primary` + 太字にする。レポート /
  設定の行も選択状態を持つ（テーマ切替とアカウントは操作なので持たない）。
  あわせて**ロゴ（説明画面への入口）のタイル地色**を状態で変えた: 説明画面を開いているときだけ
  `primary`（グリフは `primary_foreground`）で、それ以外は `muted`（グリフは
  `muted_foreground`）。ロゴは常に `primary` で塗ってあったため、説明画面を開いていることが
  サイドバーから分からなかった。
- **説明画面の Google ドライブの注意をストアのログイン手順の次へ移動**: Google ログインは
  バックアップ用で本を読むだけなら不要、という注意を、ログインの説明（ストアのログイン手順）の
  直後に置いた。あわせて配色をテーマの `warning` トークンに寄せた（直書きの `rgb` をやめ、
  ダークテーマでも読めるようにする）。
- **起動時の初期表示をサイトのログイン状態で決める**: 技術書典 / BOOTH / FANZA同人 / DLsite の
  どれにもログインしていなければ、本棚ではなく**説明画面**を開く（各ストアのログイン手順が
  書かれているので入口として機能する。本が 1 冊も入らない空の本棚を出さない）。
  **Google / GitHub だけのログインでは本棚を初期表示にしない**（連携用アカウントで本の供給元
  ではないため）。サイトに 1 つでもログイン済みならこれまでどおり本棚。
- **閲覧履歴の見出しから件数を外す**: 日付ごとにまとめて出す画面なので、全体の件数は
  情報として要らない（見出しは「閲覧履歴」だけにした）。表示中の件数は
  `flat_book_ids()`（日付 → 本を平坦化したもの）で取れるので、内部の `count` フィールドは
  削除した。

## [0.2.4] - 2026-09-20

本棚のメモリ消費を大きく削減し（アイドル表示で実測 2.34 GB → 411 MB）、見開き送りの位置ズレと
送るたびの白フラッシュを直した。FANZA の未ダウンロード本のタグを同期のたびに少しずつ取るように
なり、通知まわり（消えない通知・後から出る通知に隠れる通知）も修正している。

### Added

- **FANZA の未ダウンロード本のタグを少しずつ取得**: FANZA のタグ（ジャンル）は作品ページの
  HTML にしか無く **1 件 = 1 リクエスト**になるため、同期のたびに全件（実測 238 件）まとめて
  叩くと DMM 側の bot 判定で同期ごと失敗しうる（ログイン状態にも影響が出る）。
  同期が成功したときに **1 回 20 件まで・直列・300 ms 間隔**で取得し、
  `bookshelf_items.tags_fetched` が立った作品は次回の対象から外す（何回かの同期で埋まる）。
  失敗した作品は印を立てず次回に再試行し、セッション切れ（401/403）はその場で打ち切る。
  ヘッダーの「タグ取得」トグル（`tag.fetch.enabled`）が OFF のときは取りに行かない。
  取得できたときは「FANZA の未ダウンロードのタグ情報を N 件取得しました（残り M 件が未取得）」
  と通知する（未取得が残っているときだけ残り件数を添える）。通知は reload の**後**に出す
  （トーストは 1 つしか出せず、reload が出す通知に上書きされて隠れるため）。
  ダウンロード済みの本は取り込み時にタグを取っているので対象外（取り込み時に失敗したぶんは
  従来どおり手動の「ジャンル再取得」で拾える）。DLsite は一覧 API（`product/info/ajax`、
  20 件/リクエスト）で同期時に取れるため変更なし。

### Fixed

- **Drive 同期の「同期完了」通知が消えずに残り続ける**: 生の `set_toast` を直に呼んでいた
  経路（Drive 同期の完了・Drive 復元の結果・「Google Drive と同期しました」）は、前段で
  「消えない通知」（終了時のアップロード）を出していると `autohide = false` を引き継ぎ、
  **通知が消えずに画面に残り続けていた**（結果としてタグ取得の完了通知など、後から出る
  通知を隠していた）。`set_toast` を非公開にして `set_toast_kind` 経由に統一した。
- **タグ取得の完了通知がすぐ隠れる**: トーストは 1 つしか出せず、後から出たものが上書きする。
  進行中の通知や定期同期の完了通知が上書きしていたため、タグ取得の完了は**保護つき通知**
  （`set_protected_notice`）として出し、5 秒間（自動消滅と同じ）はバックグラウンドの通知に
  上書きされないようにした。エラー通知は保護中でも必ず出す。
- **本棚のメモリ消費（アイドルで 2.3 GB）**: アプリを起動して本棚を表示しているだけで
  ワーキングセットが 2.34 GB になっていた。原因は 3 つ。(1) 本棚は全カードぶんの表紙を
  常駐させていて、実測で 612 枚 = **507 MB**（デコード後 RGBA・448px）あった。(2)
  `reload` のたびにその 612 枚をディスクから**デコードし直していた**（ローカル本の表紙は
  pack 全体を `std::fs::read` してからネイティブサイズでデコードしていて、一時的に最大
  354 MB を確保）。(3) GPU のテクスチャを明示解放していなかった（ビューアは
  `window.drop_image` を呼んでいるが、本棚は呼んでいなかった。内蔵 GPU ではテクスチャも
  システム RAM）。
  表紙を**可視になった行から順に**読むようにし（可視 + 前後 24 件を先読み）、デコード結果は
  LRU（予算 96 MiB）で **`reload` をまたいで再利用**、予算を超えたぶんは可視を除いて古い順に
  落として `window.drop_image` で GPU ごと解放するようにした。pack の表紙は**ヘッダ +
  インデックス + 表紙エントリだけ**を読む `PackFileReader` 経由にし（pack 全体を読まない）、
  448px に縮小して持つ（リモート表紙と同じ解像度に揃える）。
- **見開き送りで画面が一瞬フラッシュする**: 見開きは 2 ページが揃うまでペア全体を白い箱で
  描く（片方だけ先に出るのを防ぐため）が、先読みが `current ± 2` までだったため、
  見開き送り（2 ページ単位）では**次の見開きの 2 枚目**が必ず未読で当たり、送るたびに
  白フレームが出ていた。見開きでは次の見開きの 2 枚まで（`current ± 3`）先読みするように
  した。単一モードの先読みは従来どおり ±2。
- **見開きモードのホイール送りが 1 ページずつズレる**: ホイールが表示モードに関係なく
  常に 1 ページ送り（`*_page_shift`）だったため、見開き（2 ページ表示）で回すと綴じ位置が
  1 枚ずつズレていった（`[0,1] -> [1,2] -> [2,3]`）。ホイールは表示モードに合った送り幅に
  なり、見開きでは **1 ノッチ = 1 見開き**（`[0,1] -> [2,3] -> [4,5]`）、単一では
  1 ページ送りのまま。見開きを 1 ページだけずらしたいときは Shift + 左右キー（従来どおり）。

## [0.2.3] - 2026-09-20

スクロールの重さを解消し（設定画面・本棚・ビューアの左パネル）、ビューアにホイールでのページ送りと
その向きの設定を追加した。付箋画面の列構成、未読数バッジやロゴの見た目、一部の表記も更新している。

### Added

- **本棚のダウンロード中止**: ダウンロード（取り込み）を途中でやめる手段が無かったため、
  コンテキストメニューの「ダウンロード中止」と `Backspace` で中止できるようにした。どちらも
  確認ダイアログ（「中止する」/「キャンセル」）を挟む。中止は**転送そのものを中断**する
  （`Transport::send_download` の進捗コールバックが `false` を返すと読み取りを止め、途中まで
  読んだバイト列は破棄して取り込まない。BOOTH と技術書典 / FANZA / DLsite で同じループを
  共有する）。進捗リングと「ダウンロード中です」の通知は即座に消え、worker の完了時に
  「ダウンロードを中止しました」と通知する。中止できるのは転送中の本だけ（取り込み＝
  レンダリングが始まった後は中断できないため、メニュー項目は実行中の本だけ有効）。取り込み
  確認モーダル待ちのときは、中止でその待ちも解放して worker を取り込みへ進ませない。
  `Backspace` はビューアーの「本棚に戻る」と同じキーだが、本棚にフォーカスがあるときだけ効く。
  `Backspace` の対象は「選択中の本」で、選択が実行中の本でなければ走っているダウンロードが
  1 件だけのときにそれを対象にする（複数走っていて選択も別の本なら何もしない）。あわせて
  **クリック / 右クリックでそのカードが選択される**ようにした（以前は選択がキーボードでしか
  動かず、クリックで始めたダウンロードを `Backspace` で中止できなかった）。
- **オープンソースライセンス表示**: 「説明」画面の一番下に「ライセンスについて」の導線を置き、
  ライセンス画面（先頭に「← 案内に戻る」）を追加した。アプリ本体（MIT）、Rust クレートではない
  同梱アセット（Lucide アイコン = ISC、PDFium = BSD-3-Clause / Apache-2.0）、依存クレート
  （名前順）を並べ、行をクリックするとライセンス全文が開く。全文は内容で重複排除して埋め込む。
  一覧と全文は `examples/regen_licenses.rs` が `Cargo.lock` とローカルの cargo キャッシュから
  生成した `assets/third-party/licenses.json` をコミットして同梱する（依存を変えたら
  `mise run licenses`。作り忘れはテスト `licenses_json_matches_cargo_lock` が検出する）。
  クレートがライセンス全文を同梱していない場合（crates.io の配布物に無い場合は多い）は、
  その旨と配布元だけを出す。

- **終了時のアップロード中であることを通知で知らせる**: 終了確認で「アップロードして終了」を
  押すと確認ダイアログは閉じるため、完了まで画面に何も残らず「終了したのか固まったのか」が
  分からなかった。実行中は Info の通知（「バックアップをアップロード中です…（完了すると
  アプリが終了します）」）を出す。長いアップロードでも見失わないよう、この通知だけは自動で
  消えない（`AppState.toast_autohide`。既定は従来どおり 5 秒で自動消滅）。

- **ビューアのホイールでページ送り**: ホイール 1 ノッチで 1 ページ進む / 戻る。1 回のイベントで
  何ページもめくらないよう、トラックパッドのような細かいデルタは 1 ノッチぶん（3 行）まで
  累積してから 1 回だけ動かす。Ctrl+ホイールのズーム、スクロール（連続）モード、ページ一覧などの
  UI パネル上では今まで通り効かない（パネル上はズームも従来どおり止めてある）。
  設定 → **「ビューア共通設定」→「ホイール 1 ノッチで 1 ページ送る向き」**で
  「下スクロールで次へ（既定）」/「上スクロールで次へ」を選べる（`viewer.wheel_direction`）。

  **既定を「下で次へ」にした根拠（OS 仕様を確認）**:
  - Windows: `WM_MOUSEWHEEL` の正は「ホイールを前方＝ユーザーから遠ざける」回転で、これが文書を
    先へ進める方向。Microsoft の仕様にも「1 ノッチ（`WHEEL_DELTA` = 120）ごとに 1 アクション」と
    あり、細かいデルタは 120 に達するまで累積する（またはスクロール粒度を決めて累積する）と
    明記されている。本実装はこの累積方式。
  - macOS: AppKit が `deltaY` / `scrollingDeltaY` を**ユーザーのナチュラルスクロール設定に従って
    自動で反転して**渡す（`isDirectionInvertedFromDevice` は「設定を無視したい場合」だけ参照する、
    と Apple のドキュメントにある）。gpui は `scrollingDeltaY` をそのまま `ScrollDelta` にして
    いるため、アプリ側で二重に反転しない。結果として両 OS とも「ユーザーにとっての下
    （＝文書の先へ進む方向）」で次へ進むのが自然になる。逆向きが好みならこの設定で反転できる。

  検証: 新規テスト 5 本（1 ノッチで次へ / 上で戻る = `wheel_scroll_turns_one_page`、
  設定で反転 = `wheel_direction_setting_flips_the_page_turn`、1 イベントで 1 ページまで =
  `wheel_one_event_turns_at_most_one_page`、連続モードでは送らない =
  `wheel_does_not_turn_pages_in_scroll_mode`、設定の保存と既定値 =
  `wheel_direction_setting_persists`）。フル 292 件全緑、clippy -D warnings / fmt クリーン。

- **サイドバーに「お気に入り」を追加**: お気に入りの本をまとめて見る導線が無かった（本棚の
  絞り込みボタンを押すか、お気に入りタグを辿るしかなかった）ため、サイドバーの**本棚の下**
  （メニューの「表示」も同じ並び）に「お気に入り」を足した。画面は**本棚と同じビュー**を
  共用し、**お気に入り固定**で表示する（検索・サイト絞り込み・並び替え・カード / リストの
  切替は本棚と共通のまま）。画面そのものが絞り込みなので**お気に入りの絞り込みボタンは
  出さず**、「全項目」で解除してもお気に入りは外れない。見出しは「お気に入り」+ 注意書き
  「お気に入りにした本は、同期時に未ダウンロードだと自動でダウンロードされます」。
  あわせて、この注意書きどおりになるよう**自動ダウンロードの契機を増やした**: 以前は起動時
  （本棚の初回描画）だけだったので、BOOTH / FANZA / DLsite / 技術書典の**同期が終わった
  とき**にも落とすようにし、同期のたびに呼んでも同じ本を二重に積まないようにした。
  サイトスコープ（サイドバーの本棚の下のサブメニュー = 本棚の閲覧スコープ）はお気に入り画面には
  適用しない（**すべてのサイトのお気に入り**を出す）。適用すると、本棚が「技術書典」のときに
  お気に入り画面が 0 件になり、お気に入り画面にはサイトを出す見出しが無いため理由が
  分からなくなる（実データでは booth 1 / dlsite 1 / fanza 3 のお気に入りがあった）。
  お気に入り画面では**「タグ取得」と「同期」も出さない**（本棚側の操作のため。表示切替・
  並び替え・検索・各絞り込みは本棚と同じに使える）。

  検証: 新規テスト 6 本（お気に入り固定 = `favorites_only_locks_the_favorite_filter` /
  サイトスコープを無視 = `favorites_only_ignores_the_shelf_site_scope` /
  絞り込みボタンと注意書き = `favorites_screen_hides_the_favorite_filter_and_shows_the_note` /
  タグ取得・同期を出さない = `favorites_screen_hides_tag_fetch_and_sync` /
  並び順 = `favorites_row_sits_below_bookshelf` / 二重に積まない =
  `auto_download_does_not_queue_the_same_book_twice`）。未読数のテストにも
  「お気に入り画面では出さない」を追加。cargo test -p thundoku-shelf 284 件全緑、
  clippy -D warnings / fmt クリーン。

### Changed

- **付箋画面の行の並びを変える**: 1 列目を付箋ページの画像から**本の表紙**にし、付箋ページは
  タグ列の右へ移した。左から **表紙 / 情報 / タグ / 付箋ページ / メモ / 本を見る** の 6 列に
  なる。表紙は本棚のリストと同じ枠（200x133）と同じ解決順（サイトの表紙キャッシュ →
  pack の cover → プレースホルダ）で、付箋ページと同じ枠の大きさ。画像はどちらも
  **表示中の行だけ**背景でデコードし、LRU（64 件）を共有する（上限を超えた行は
  プレースホルダのまま）。
- **本棚の表示形式（カード / リスト）を保存する**: 切り替えても次に開くとカード形式に戻って
  いたため（表示形式は永続化していなかった）、設定キー `bookshelf.view_mode`（`"card"` /
  `"list"`）に保存して次回起動で復元するようにした。並び替え（`bookshelf.sort_field` /
  `bookshelf.sort_ascending`）と同じ扱い。未設定・未知の値はカード形式（従来の既定）。
- **綴じ方向（右綴じ / 左綴じ）を本ごとに持てるようにする**: 既定はサイト単位の設定
  （`viewer.page_turn.{site}`）のままだが、ビューアの「右綴じ / 左綴じ」ボタンで変えた分は
  **その本だけ**に効くようにした（`books.page_turn`。本ごとの指定 > サイト別設定 > グローバル >
  サイト既定の順に解決する）。サイト別の既定は書き換えないので、設定画面の「サイト別 Viewer
  設定」は変わらず他の本の既定として働く。同じサイトでも綴じが違う本を1冊だけ直せる。
  本に紐づかない表示（チェックリストの試し読み）では保存せず、その場限りになる。
  既存 DB には `migrate()` が列を足す（リリース前のためマイグレーションファイルは増やしていない）
- **読書状態の「読書中」を「読んでいる途中」に変える**: 未読 / 読書中 / 読了 の 3 状態のうち
  「読書中」だけ何を指すのか伝わりにくかったため、表示ラベルを「読んでいる途中」にした
  （本棚のカードバッジ・リストの状態チップ・フィルタ、閲覧履歴、付箋、設定の冊数集計、
  説明画面の文言）。判定（`ReadingState::from_progress`）と色（青）は変えていない。
  リストのチップは他の長いタグと違って折り返さない（状態の意味を優先）。
- **技術書典に未ログインならサイドバーとメニューの「チェックリスト」を使えなくする**:
  チェックリストは技術書典専用で、同期もお気に入り取り込みもログインが要る（未ログインでは
  「ログインしてから同期してください」で弾かれる）。導線だけ出ている状態をやめ、サイドバーは
  行ごと隠し（ログアウトで消える）、メニューの「チェックリスト」は無効にする（項目が消えると
  並びが動くため）。可否の判定は `sync_app_menus` に集約し、技術書典のログイン状態の変化は
  起動時のログイン監視（100 ms 間隔）が検出してメニューを組み直す。
- **PDF レンダリングを全プラットフォーム PDFium に統一**: macOS / Linux は mupdf をソースビルドして
  使っていたが、mupdf は **AGPL-3.0** で MIT 配布の本アプリと両立しないため依存から削除した
  （Windows だけが使っていた `pdfium-render` に寄せ、ターゲットごとの分岐も無くした）。PDFium は
  **実行時ロード**（静的リンクしない。prebuilt の static は macOS で `FPDF_FORMFILL` を欠きリンク
  できない）とし、macOS は `.app/Contents/Frameworks/libpdfium.dylib`、Windows は従来どおり exe と
  同じフォルダの `pdfium.dll` を同梱する（どちらも `chromium/7881` を SHA256 検証つきで取得）。
  PDFium の呼び出しはプロセス全体の `Mutex` で直列化する一方、WebP エンコードは 8 スレッドで並列に
  流すため速度は据え置き（12 ページのサンプルで mupdf 142ms / PDFium 146〜199ms）。

- **進行中の通知に Spinner を出す**: 終了時のバックアップのアップロード・ダウンロード・
  取り込みのように「終わるまで動き続ける」処理で、通知に Spinner を添えるようにした。
  通知の面と文字だけでは「進んでいるのか固まっているのか」が分からず、止まって見えた。
  進行中の通知は同じ id で**置き換わる**ため、進捗のたびに積み直しても通知は増えない。
  ただし置き換えのたびに入場アニメ（400ms）がやり直されるので、文言に割合は入れず
  「何件・どの段階か」だけを伝え、積む間隔も最短 10 秒あける（件数と段階は 1 冊ごとに変わるため、一括同期でも 10 秒に 1 回で収まる）（実測: 1 冊の取り込みで
  8 秒間に 28 回積まれ、通知が流れ続けて見えていた）。
  ダウンロード/取り込みは従来カードのリングだけだったので、一覧をスクロール中でも
  何が動いているのか分かるようになった。

- **ダウンロード中/取り込み中の通知**: 開始・進捗・完了で通知を出す。ダウンロードは
  「ダウンロード中です…（2 件 / 40%）」、取り込み段階に入ると「取り込み中です…」に変わる。

### Fixed

- **Release ワークフローの pdfium 取得が macOS で 404 になる問題**: macOS のステップだけ
  アセット名を `pdfium-${asset}.tgz` と組み立てていたが、`asset` 側に既に `pdfium-` が
  入っていたため **`pdfium-pdfium-mac-arm64.tgz`** を要求して 404 になっていた
  （Windows のステップは `pdfium-win-x64.tgz` と正しい名前を直書きしていたため成功していた）。
  参照を `${asset}.tgz` に直し、あわせて混雑時のための**再試行（15 秒 / 30 秒）**と
  **API 経由（アセット ID 指定）へのフォールバック**を入れた。取得物の SHA256 検証は維持。
- **タイトルバーが埋もれていて信号機が押せない**: `TitleBar::window_options()` は透過タイトルバー +
  `app_owns_titlebar_drag` を指定する（＝タイトルバーはアプリが描く契約）。macOS ではタイトルバーを
  描いていなかったため、信号機（閉じる / 最小化 / 最大化）が本棚のヘッダーに重なって押せず、
  ウィンドウをドラッグする領域も無くなっていた。gpui-kit の `TitleBar` を置き、左側（信号機の右
  80 px から）にウィンドウ名を出す。システムメニューバーがメニューを持つので、空いた左側を
  ウィンドウ名に使う（gpui-kit の story アプリと同じ流儀）。あわせて、リーダーのオーバーレイ
  上端と画像のフィット計算を `workspace::TITLE_BAR_HEIGHT` に一本化した（従来は macOS も
  36 px 固定で、タイトルバーの 34 px と 2 px ずれて本棚のヘッダーが細く覗いていた。フィット
  計算の `WIN_TITLE_BAR_HEIGHT` も macOS は 0 で表示領域と食い違っていた）。サイドバー側に
  残っていた macOS 用の上余白 30 px も外した（信号機を避けるための余白で、タイトルバーが
  確保した後は二重になり、ロゴ行だけが 30 px 下がっていた）
- **起動時の「Drive バックアップが見つかりました」がローカル側の変更だけで出る**: 判定が
  「ローカルと Drive が違う」だけだったため、他端末を起動していなくても、この端末で読書
  した分だけで復元確認が出ていた（「復元する」を押すと新しいローカル側の進捗が古い
  バックアップで上書きされる）。最後にアップロードしたバックアップの正規形 md5 を
  `app_settings['drive.backup.md5']` に保存し、**Drive 側がそれから動いたときだけ**復元を
  提案するようにした（`inspect_drive_backup` / `BackupStatus::should_offer_restore`）。
  比較は、同期のたびにアプリ自身が書き換える時刻列（`books.updated_at` /
  `bookshelf_items.synced_at,updated_at` / `tbf_events.updated_at`）を落とし、行を PK 順に
  並べた正規形で行う（時刻だけの変化や行の物理順で差分にならない）。バックアップの書き出しも
  PK 順の `ORDER BY` にして、無変更なのに再アップロードになるのを防ぐ。アップロード後と
  復元後は基準値を更新する（復元後に更新しないと、ローカルに Drive に無い行が残っている限り
  毎回「復元しますか」と聞き続ける）。
- **取り込み中に別の本のビューアーが開いてしまう**: ある本をダウンロードしている間に、
  すでにダウンロード済みの別の本をクリックすると、その本がビューアーで開いていた
  （取り込みの書き込みとビューアーの読み込みが重なる）。取り込み中はビューアーを開かない
  ようにし（対象が未ダウンロードなら取り込みの確認へは進む）、理由を Info の通知で伝える。
  右クリックメニューの「開く」も同じ条件で無効にする。クリック経路は `open_or_download` に
  寄せたが、**未ダウンロード本の扱いは従来どおり経路ごとに違う**（行・関連サムネイルは
  確認してから取り込む / カードと Enter はそのまま取り込む）ので、`confirm_undownloaded` で
  指定する。
- **アップロード中に閉じるボタンで終了できてしまう**: 終了確認で「アップロードして終了」を
  押した後も、ウィンドウの閉じるボタン（macOS はメニューの「終了」）がそのまま効き、
  進行中のアップロードを中断させて終了できた。アップロード中は閉じる要求を受け付けず
  （`Workspace::handle_window_close_request`）、macOS ではメニューの「終了」も無効化する
  （`app_menus(false)`。アップロード完了で戻す）。
- **取り込み内容の確認ダイアログが画面からはみ出して押せない / リストがスクロールしない**:
  選択肢（コンテンツ）が多いとダイアログが画面より縦に伸び、「この内容で取り込む」が画面外に
  出ていた。高さの上限を選択肢リストに掛けたが、`gpui-kit` の `overflow_y_scrollbar` は
  要素のサイズ指定をスクロール領域側へ移すため、中身に `max_h` を残すと「中身＝ビューポート」
  と見なされてスクロールの余地が 0 になり（行も縦に縮む）、後ろのコンテンツを選べなかった。
  高さは外側の箱（`h`）に持たせ、スクロール領域を `flex_1`、行を `flex_shrink_0` にした
  （`Scrollable` が前提にしている形）。ウィンドウが低いときはリストの上限をウィンドウ高の
  半分まで抑える。作り忘れないよう、スクロールの余地と行の高さをテストで見張る。
- **取り込み内容の確認ダイアログのバツが効かない**: バツは押しても閉じない（worker が回答を
  待っているため）ので、置かないようにした（`Dialog::close_button(false)`）。
- **取り込み内容の確認がビューアーの下に隠れる**: ビューアー表示中に取り込み確認が来ると、
  モーダルはビューアーより下の層に描かれるため選択肢が見えず操作できなかった。確認が来たら
  ビューアーを閉じ、確認が出ている間はビューアーを開かないようにした（`open_reader` 系）。
- **ダークモードでダイアログが背景と見分けられない**: ダイアログの面が背景と同色
  （neutral-950）で、膜を重ねても境界が見えなかった。ダークでは面を一段明るい
  neutral-800 にし、膜を濃く（黒 45%）した（Web のダークモードのモーダルの標準的な
  配色に合わせる。`components/dialog.rs` の色関数 + テスト）。
- **本棚で絞り込みを解除するとスクロールが先頭に戻る**: 絞り込み（タグ / イベント /
  サークル / 作者 / 既読 / 検索）を適用すると一覧の件数が変わり、仮想化リストの
  `ListState::reset` が必要になる。これがスクロールのアンカーを捨てるため、適用のたびに
  先頭へ戻り、解除しても元の位置を失っていた。件数を差し替える直前に位置を
  「先頭からの割合」で控え、差し替え後に同じ位置を指し直すようにした
  （`set_list_count_keeping_scroll`）。絞り込みの適用では先頭に戻らず、解除では
  絞り込む前の位置に戻る。カード表示 / リスト表示で別々のリスト状態を持つため両方に適用。
- **終了時にアップロードしたのに、次の起動で毎回「Drive のバックアップを復元しますか？」と
  聞かれる**: Google のプロフィール（`sub`）は設定画面を開くまで取得されない（`userinfo` は
  ネットワーク取得）ため、起動直後の `AppState.google_profile` が空だった。この状態では
  1. 終了時のアップロードが `identity_sub = None` になり、`thundoku-backup.json`（DB
     バックアップ）を**1 バイトも上げずに**終了していた（ログに残らず、画面は普通に閉じる）、
  2. 起動時の復元確認は所有者フィルタを組めないままローカル全件で比較し、Drive 側
     （所有者で絞られた古いバックアップ）と範囲が食い違って**必ず「差分あり」**になっていた、
  という二重の不具合だった。
  プロフィールを取得時に keyring（`google-profile`）へ保存し、起動時に復元するようにした
  （`AppState::restore_google_profile`）。保存が無い場合は起動時に `userinfo` を取りに行き、
  ログアウト・トークン失効では控えも削除する。加えて、所有者が特定できないときは差分比較
  自体を行わない（`Workspace::backup_owner_ids`）ため、比較範囲の食い違いで復元確認が
  出ることはなくなり、owner 不明のまま DB バックアップのアップロードをスキップした場合は
  警告ログを残す。
  副次的に、起動直後（設定画面を開く前）の本棚の所有者フィルタ・パックの復号・Drive 同期も
  正しい sub で動くようになった（従来はプロフィール未取得のため「未所属の本だけ」が見えていた）。

- **ダークモードのダイアログ・通知の視認性**: ライブラリ既定のダークパレットは面の階層が
  潰れていて（`popover` が背景と同色、`border` がカードやダイアログの面と同色、`input` が
  背景とほぼ同色）、起動時の復元確認・取り込み確認・通知が背景に溶けていた。ダークだけ
  面の階層をアプリ側で引き直し（`theme::apply_dark_surfaces`。`popover` = neutral-900、
  `border` / `input` = neutral-700、`overlay` = 黒 45%）。ダイアログの面は当初は背景色のまま
  縁と膜と影で浮かせる方針だったが、描画した画素を測ると分離が弱かった（ダークで
  面 #0a0a0a vs 膜を重ねた背景 #060606 = 1.02:1）ため、`popover`（neutral-900）に
  統一した（下記「確認ダイアログが背景と同化して気づかない」を参照）。ダイアログの副ボタン（キャンセル /
  保存せずにアプリ終了）は塗りが面とほぼ同じで形が見えなくなっていたため、縁を付ける
  （`dialog_button`）。

- **確認ダイアログが背景と同化して気づかない**: ライトモードは膜（`overlay`）が 5% しかなく、
  ダイアログの面も背景と同じ白のため、膜・縁・影のうち膜がほぼ効かず背景に溶けていた
  （面 #ffffff vs 膜を重ねた背景 #f2f2f2 = 明度差 0.05 未満）。ライトは面を背景より明るく
  する余地が無い（面がすでに白）ので、**膜を濃くする**方を直した。

  - ライト: 膜 5% → 50%、縁を `neutral-300` に（白い面の上で `#e5e5e5` はほとんど見えない）。
    ライトには上書き自体が無かったので新設（`theme::apply_light_surfaces`）
  - ダーク: 膜 45% → 55%、縁を `neutral-700` → `neutral-600` に（ダークの面の階層は
    通知・ポップオーバー側を先に修正済み）
  - **ダイアログの面を「浮いた面」(`popover`) に統一**: アプリ自作のダイアログと
    ライブラリの `Dialog` 5 箇所（取り込み内容の確認・ダウンロード確認・ダウンロード中止・
    起動時の復元確認・Drive 同期）を同じ面に揃えた。ページより 4〜8 明度ポイント明るくなる

  実測（描画した PNG の画素を ffmpeg で取得し、テーマのテストと同じ明度差の尺度で比較。
  暗色はコントラスト比だと 1.1 前後に見えて誤判定しやすい）:

  - ライト: 面 `#FFFFFF` vs 膜を重ねた背景 `#7A7A7A` = **明度差 0.522**（コントラスト比
    4.29:1。修正前は面 #ffffff vs 膜後 #f2f2f2 = 0.05 未満）
  - ダーク: 面 `#171717` vs ページ `#0A0A0A` = **明度差 0.051**（ページから +5.7 明度ポイント）

  両モードの明度差をテストで固定した（`theme.rs`。ライトは修正前 0.05 で失敗する）。

- **本棚のスクロールが重い（1 ノッチあたり約 90 クエリ）**: `BookshelfView::render` の中で、
  サイト絞り込み中のお気に入りタグを**本 1 冊ずつ** `book_tags` から作っていた
  （`bookshelf_items` 1 回 + `books` 1 回 + 対象サイトの冊数ぶん `book_tags`）。スクロールは
  1 ノッチごとに描画が走るため、UI スレッドで 1 ノッチあたり約 90 クエリが走っていた。
  リーダーの**左パネル**（画像一覧）のスクロールが重かったのも同じ経路だった
  （リーダーは本棚を消さずに上へ重ねるため、背後で本棚の描画が走り続けている）。
  - サイト別のお気に入りタグは `site_favorite_tags` にキャッシュし、フィルタ状態が変わった
    とき（`rebuild_filtered`）だけ作り直す。描画はキャッシュを並べ替えるだけ
  - タグは core の `db::tags::tag_names_by_book` で **1 クエリ**にまとめた（`reload` の
    タグ取得も同じ関数に置き換え。以前は 1 冊ずつ `list_for_book` を呼んでいた）

  あわせて、**毎フレーム `filtered_dirty` が立っていた**のも直した（`current_search` は空の
  とき `None` を返すのに `Some("")` と比較していたため、検索していなくても毎描画でフィルタの
  作り直し＝サイト別タグの作り直しが走っていた）。`None` と空文字を同じ扱いにした。

  検証: 新規テスト `site_tags_are_cached_between_draws`（描画では読み直さない / 読み直せば
  反映される。毎フレーム再計算する不具合を検出できる形にしてある）。本棚の `render` から
  DB 呼び出しが消えたことを静的に確認。実機の実測で、本棚を 12 ノッチスクロールしたときの
  クエリが **899 → 0**（`book_tags` は 672 → 0）。フル 297 件 + core 130 件全緑、
  clippy -D warnings / fmt クリーン。
- **小ネタ 4 件（ビューア / サイドバー）**: 積み残しをまとめて直した。
  - **見開きでもホイール 1 ノッチ = 1 ページ**: 直前のリリースでは「次へ」ボタンと同じ 1 見開き
    単位だったので、`next_page_shift` / `prev_page_shift`（見開きでも 1 ページ動く版）に変更。
    実機で見開きのまま 13 → 14 → 13 を確認。
  - **サイドバーのロゴのグリフを 24px に**: 箱はナビ行と同じ 36×36 なのにグリフだけ 20px で、
    ロゴのマークだけ小さく見えていた。ナビ行のアイコンと同じ 24px に揃えた。
  - **未読数バッジの文字色**: 地色（赤 / 黄 / 緑）はテーマに依らない固定色なのに、文字色に
    `primary_foreground` を使っていたため、ライトテーマでは白になり、黄の上でコントラスト
    が 1.9:1 まで落ちて読めなかった。固定の濃色（`0x0a0a0a`）にして 赤 5.6:1 / 黄 10.9:1 /
    緑 8.3:1 に。WCAG のコントラスト比を検証するテストを足した。
  - **サイドバーの描画が毎回 `theme.mode` を DB から読んでいた**: `theme_mode_name` を
    フィールドにキャッシュし、起動時（`restore_theme_mode`）と切替時（`set_theme`）だけ
    更新するようにした。実機の実測で、設定画面の 12 ノッチスクロール中の `app_settings`
    読みが **約 72 → 0** に。

  検証: 新規テスト 4 本（見開き = `wheel_scroll_turns_a_single_page_in_spread_mode` /
  グリフ = `sidebar_logo_glyph_is_as_large_as_nav_icons` / コントラスト =
  `unread_badge_text_is_legible_on_every_badge_color` / テーマキャッシュ =
  `theme_mode_is_cached_and_updated_on_change`）。フル 296 件全緑、clippy -D warnings /
  fmt クリーン、release ビルド成功。
- **本棚カードの表紙バッジ（お気に入り / ダウンロード）が読めない**: 文字グリフ（`♥ ♡ ✓ ↓`）を
  14 px で描き、塗りは 20〜25% の半透明だったため、**明るい表紙の上では 1.6〜3.0:1** まで
  コントラストが落ちて沈んでいた（中間色の表紙では 1.1:1 = ほぼ見えない）。アイコン
  （`AppIcon::Heart` / `HeartFilled` / `Download` と gpui-component の `Check`）に替え、塗りを
  **オフ = 60% 黒のスクリム + 白アイコン**（最も明るい白表紙の上でも 5.7:1）、**オン =
  不透明の色チップ + 白アイコン**（お気に入り rose-600 = 4.7:1 / ダウンロード済み
  emerald-700 = 5.5:1）にした。半透明の色チップは明るい表紙で色が飛ぶため、オンは不透明に
  する。輪郭は 35% 白で、黒い表紙でも丸の形が残る。表紙は任意の画像なので配色はテーマではなく
  「どんな表紙の上でも読めるか」で決める（両テーマ共通）。
  状態は色だけでなく**形**でも変わるようにし（WCAG 1.4.1）、ホバーで「お気に入り（クリックで
  解除）/ お気に入りにする / ダウンロード済み / 未ダウンロード（カードをクリックで取り込み）」を
  出す。コントラストは白 / 黒の表紙に合成して
  `cover_badge_icons_stay_readable_on_any_cover`（WCAG 2.2 SC 1.4.11 の 3:1 より厳しい
  4.5:1）で固定した。
- **閲覧履歴カードの表紙バッジも本棚と共通にする**: 履歴のカードは、お気に入りハートが
  `absolute()` を持たず**表紙の下（レイアウトの流れ）に落ちていた**（実測: 表紙が
  `60, 99.5 124.5x166` のときハートは `60, 265.5 24x24` = 表紙の底と同じ位置で、重なって
  いなかった）。右下のダウンロードバッジも本棚と同じ低コントラスト（20% 緑 + 緑の `✓`）だった。
  カードの表紙オーバーレイを本棚と共有の `CoverBadge` / `cover_badge()` に置き換え
  （右上 = お気に入り + クリックでトグル / 右下 = ダウンロード済み。履歴の本は必ずローカル本
  なので常に「済み」）、**表紙の枠の内側**に乗るようにした。表紙の上で読める配色・状態で形が
  変わること・ホバー説明は本棚と自動的に揃う（共有のため）。位置は
  `history_card_badges_sit_inside_the_cover`（表紙の bounds の内側か）で固定した。
  リスト行のハート（`render_heart`）は表紙ではなく行の背景の上なのでテーマ色のままとした
  （→ 次の項目で共有パレットに揃えた）。
- **リスト行の状態アイコン（ダウンロード済み / 未ダウンロード / お気に入り）を読みやすくする**:
  16 px の細いアイコンなのに、ライトテーマの行の背景（`muted`、選択中は `secondary`）に対して
  `#059669` = **2.99:1** / `#b45309` = 3.99:1 / `#db2777` = 3.65:1 / `muted_foreground` = 3.76:1
  しかなく沈んでいた（ダークは元から 5.7〜9.1:1 あり据え置き）。表紙の上に乗るカードのバッジと
  同じ **4.5:1**（WCAG 2.2 SC 1.4.11 の 3:1 より厳しい）を目標に、行の背景の上に描く 3 状態の
  色を共有パレット `StatusIconPalette`（テーマで変わる。ライト `#036e51` / `#92400e` /
  `#be185d` / `#616161`、ダーク `#34d399` / `#fbbf24` / `#f472b6` / `#a3a3a3`）に集約した。
  本棚の行の 3 アイコンと、閲覧履歴の**リスト行**のお気に入りハートが同じ色になる
  （履歴のハートの丸の地色は `background`）。コントラストはテーマの実値を使って
  `status_icons_are_readable_on_list_rows`（両テーマ / 未選択と選択中の行）で固定し、
  色が本棚と一致することは `history_row_heart_reuses_the_shelf_status_colors` で固定した。
  履歴のリスト行のハートにホバー説明（「お気に入り（クリックで解除）/ お気に入りにする」）を
  足して、本棚の行と揃えた。
- **未読数が本棚以外の画面でも出ていた**: サイドバーの未読数は「本棚のもの」なのに、閲覧履歴 /
  付箋 / 設定などを開いても出ていた（サイドバーを開いたときのヘッダーの「未読数 N 件」と、
  閉じたときのロゴのバッジの両方）。**本棚のときだけ**出すようにした。未読数の取得自体は
  全画面で続け、表示だけをゲートする（開 / 閉の両方で同じ判定を通す）。本棚・閲覧履歴・付箋・
  チェックリスト・設定・説明・レポート × 開 / 閉 の組み合わせを
  `unread_count_shows_only_on_the_bookshelf_screen` で固定した。
- **ライセンス画面が削除済みの MuPDF を案内していた**: 説明文が「OS ごとに使うもの
  （Windows の PDFium、macOS の MuPDF など）も含みます」のままだった。PDF 表示は
  **全プラットフォーム PDFium に統一**して MuPDF（AGPL-3.0）は依存（`Cargo.toml` /
  `Cargo.lock`）からもライセンス一覧からも消えているので、**同梱していないライブラリを
  名指ししている**状態だった。Rust のクレートと同梱アセット（PDF 表示に使う PDFium、
  アイコンの Lucide）を挙げる文面に直し、説明文が名指しするものはライセンス一覧に載っている
  ものだけであること（+ 削除済みの名前を書かないこと）を
  `licenses_lead_names_only_bundled_libraries` で固定した。
- **説明画面のロゴの角丸が枠やアイコンの枠線と揃っていなかった**: ロゴ画像（アプリアイコン）は
  白地の内側に濃紺の枠線が描かれているのに、画像に角丸が無かったため**白い角がはみ出して**
  見えていた（`overflow_hidden` では画像の角は切れない）。次に `rounded_xl`（12px）で角丸を
  入れると、今度は**枠線の角が削れて**見えた（枠線の外側の丸みは画像サイズの 8% = 1024px で
  81.9px、64px 表示で 5.12px）。アセットから測った半径 `APP_LOGO_RADIUS` を画像と枠の両方に
  使って、白い角も枠線の削れも出ないようにした（実測: 角の画素が白 `255,255,255` →
  背景 `10,10,10`。角でも枠線は途切れない）。アイコンを差し替えたら気づけるよう、アセットから
  測った半径と定数の一致を `about_logo_radius_matches_the_icon_frame` で固定している
  （描画結果の画素は、テスト用プラットフォームに `HeadlessRenderer` が無く
  `window.render_to_image()` が使えないため見られない = 実機のスクリーンショットで確認）。
- **サイドバーのロゴ（白いタイル）がナビ行の下地より大きかった**: 閉じたサイドバーでロゴだけ
  40×40、ナビ行のホバー / 選択で出る下地（`secondary` の角丸）が 36×36 だったため、白い
  ブロックだけ一回り大きく浮いて見えていた（実測: 40×40 = 下地より 4px 大きい）。閉状態の
  ロゴを **36×36** にして下地と揃えた（実測: 白タイル 36px / 下地 36px。角丸も同じ
  `rounded_xl` なので形まで一致する）。開状態はアプリ名の隣に出るので 40×40 のまま。
  あわせて、サイズを測っていない（自分で設定した `sidebar_open` を assert するだけの）
  `sidebar_logo_stays_square_when_collapsed` / `sidebar_logo_stays_square_when_expanded` を
  削除した（コメントの「常に 40×40」も実測していなかった）。
- **未読数バッジがロゴのタイルを覆っていた**: 閉じたサイドバーのロゴ（36×36）の右上に載る
  未読数バッジが、10px フォント + 4px 余白 + 桁数無制限だったため、3 桁（272 件）で
  **28×16 = タイル面積の 35%** になり、タイルの右上 67% を覆ってロゴのグリフの上端まで
  隠していた（本が増えるほど悪化する構造。4 桁で ~34px = タイル幅の 94%、5 桁で ~40px =
  タイルより広い）。9px フォント / 3px 余白 / 14px 角に詰めて **23×14 = 25%** に収め、
  ラベルは 3 桁で頭打ち（`99+`）にした。「99+」は「99 より多い」を保ったまま 3 グリフ
  （＝従来と同じ幅）に収まる最大の表現で、「99+ にすれば小さくなる」わけではない
  （幅を決めているのは桁数そのもの）。開いたサイドバーの「未読数 N 件」は幅に余裕があるので
  正確な数をそのまま出す。
  検証: 描画パスを回してバッジとタイルの実寸を測るテスト（修正前 30×16 = 37% → 修正後
  23×14 = 25%。実機の実測は 28×16 = 35% で、テスト用フォントでもほぼ一致する）。
- **設定画面のスクロールがもっさりする**: `SettingsView::render` の中で毎回 DB を読んでいた
  （`refresh_status_counts` が全書籍ぶんの進捗を 1 冊ずつ引く N+1、非表示リストの再取得、
  表紙キャッシュの有無を 1 件ずつ `Path::exists()`、`drive.*` / `api.*` の設定 4 件）。
  スクロールでは 1 ノッチごとに描画が走るため、UI スレッドで **1 ノッチあたり約 600 クエリ**
  （設定画面を開くだけで 3,139 クエリ）が走って引っかかっていた（同条件の閲覧履歴は
  12 ノッチで 15 クエリ）。
  状態の読み込みは `SettingsView::reload()` にまとめ、**画面に入ったとき**
  （`Workspace::switch_to`。閲覧履歴 / 付箋と同じ扱い）と、**この画面の操作でデータが
  変わったとき**（Drive 同期完了・ローカルデータ削除）だけ読むようにした。描画はキャッシュ
  を出すだけ。実測: 設定を開く **3,139 → 293 クエリ** / スクロール 12 ノッチ
  **7,261 → 82 クエリ**。残りは 1 ノッチあたり約 6 件の `app_settings` 読み（サイドバーの
  描画が `theme.mode` を毎回読んでいる分など）で、体感に影響しないレベル。
- **`sidebar_notes_row_opens_the_notes_screen` が稀に落ちる**: サイドバーの幅は開閉
  アニメーション（180ms）で動くのに、4 フレーム描画しただけで行の位置を測ってクリックして
  いたため、位置がずれて付箋に切り替わらないことがあった（フルスイートで再現。単体では
  通る）。既存の `bookshelf_submenu_fits_all_rows` と同じく、位置が変わらなくなるまで
  `advance_clock` + 描画してから測るようにした（フル 6 回連続で安定）。

## [0.2.2] - 2026-09-15

配布物の作り方の修正（コードの挙動は 0.2.0 と同じ）。

- **macOS の `.app` をバンドルとして ad-hoc 署名**してから zip 化する。バンドルの
  組み立て直後は実行ファイル単体の署名（リンカが付ける）しか無く
  `_CodeSignature/CodeResources` が無いため、`codesign --verify` が失敗し、
  Gatekeeper も「壊れているため開けません」側の判定になっていた。署名直後に
  `codesign --verify` して、壊れていればジョブを失敗させる

### Changed

- **macOS の配布物を `.app` バンドル化**: 生バイナリ 1 個を配っていたのを、
  `Info.plist`（バンドル ID・表示名・最小 OS バージョン・バージョン）と
  アプリアイコン入りの `ThundokuShelf.app` に変更した。Finder からアプリとして
  扱え、Dock の表示も正しくなる（`ditto -c -k --keepParent` で権限を保って zip 化）
- **成果物名にバージョンを付与**: `thundoku-shelf-0.2.2-aarch64-macos.zip` のように
  バージョンを含める（複数バージョンを落としても区別できる）
- **リリースバイナリを strip**: `[profile.release] strip = true` でシンボルを落とし
  配布サイズを削減
- **`pdfium.dll` の供給網対策**: Windows 版は実行時に読み込む `pdfium.dll` を
  第三者のリリースから取得しているため、**SHA256 を検証してから展開**するようにした

## [0.2.0] - 2026-09-15

### Added

- **GitHub ログイン + レポート（Issue 投稿）**: サイドバーの「設定」の上に「レポート」を出し
  （GitHub ログイン時のみ）、アプリからこのプロジェクトの Issue を立てられるようにした
  - 認証は **OAuth Device Flow**（RFC 8628）。デスクトップアプリにクライアントシークレットを
    置けない（GitHub が禁じている）ため `client_id` だけで完結する方式を採用。
    `DEFAULT_GITHUB_CLIENT_ID`（`THUNDOKU_GITHUB_CLIENT_ID` で上書き可）のみ埋め込み、
    **secret は一切保持しない**
  - トークンは keyring（`USER_GITHUB`）に保存して起動時に復元する。**OAuth App 側で
    Device Flow の有効化が必要**（未設定だと `device_flow_disabled`）
  - レポート画面: テンプレート（`.github/ISSUE_TEMPLATE/*.yml` を Contents API で取得）を
    選ぶとタイトルと本文が埋まり、タイトル・本文（複数行）を編集して画像を添付し、送信で
    Issue を作成する。**投稿先はこのアプリのリポジトリ（`MegaBlackLabel/thundoku-shelf-app`）
    に固定**する（宛先を利用者が差し替えられると、誘導された利用者に別のリポジトリへ文面と
    スクリーンショットを送らせる余地になるため）
  - 画像は `uploads.github.com/user-attachments/assets`（`gh` CLI と同じ内部 API）で上げて
    本文に `![](url)` を挿入する。**write 権限が無いリポジトリでは画像なしで投稿できる**
    （`data:` URI は GitHub のサニタイザで除去されるため使えない）
  - スコープは `public_repo` のみ。**private リポジトリには投稿できない**（エラー文言に明記）
  - 実測（本番 GitHub に対して実装コードで通した）: Device Flow → `GET /user` →
    `repository_id` → 画像アップロード（201）→ Issue 作成（201、本文の画像は署名付き
    `private-user-images` に書き換わって表示される）
  - 画像は 1 件 10MB まで。alt に使うファイル名は Markdown を壊さないようサニタイズする
  - トークンの失効（401）はログインをやり直せる状態に戻し、回数制限（429 / `Retry-After` 付き
    403）は待ち時間を画面に出す。keyring の読み書きは背景で行い、OS の応答待ちで UI を固めない
- 本棚リスト表示を行の 4 列レイアウトに改修:
  - **表紙と情報列を固定サイズ**（表紙 162x108 / 情報列 320px）にし、行の高さが内容で
    変わってもテキスト開始 X が全行で揃うようにした（旧: 表紙が行の高さに追従して
    幅が行ごとに変わり、テキスト開始位置がずれていた）
  - **未読 / 既読 / ダウンロード済み / お気に入り**をサムネイル上のバッジから**ページ数の下**へ移動。
    文字は短い「未読 / 既読」だけ四角い `Tag` にし、長い「ダウンロード済み / 未ダウンロード」
    「お気に入り」は 16px のアイコン（`HardDrive` / `Cloud` / `HeartFilled`・`Heart`）+ tooltip で表示。
  - お気に入りは登録 / 未登録の両方を出す（未登録は控えめな輪郭ハート）。
    アイコンの**クリックでお気に入りをトグル**する（`bookshelf_items` + `books` を更新。
    行クリックへ伝播させないのでビューアー / ダウンロードは走らない）
  - **タグ情報エリアをタグ列として行幅の約 20% に固定**し、その中で折り返すようにした
  - タグ情報エリアの右側に**関連書籍カルーセル**（同一サークル → 同一作者、最大 5 件）を追加。
    カルーセルは行の残り幅いっぱいを使い、サムネイルはリストの表紙と同じ大きさ・比率
    （162x108、比率を保って収める = 切り抜きなし）。ツールチップで作品名、
    クリックでビューアー（ローカル本）/ ダウンロード（未取得）
 - カルーセルの前へ / 次へを**自前の四角いバー**に変更（高さは列＝サムネイルと同じ、幅 26px、
 tooltip は日本語の「前へ / 次へ」。移動できない向きは薄く表示）。組み込みのコントロールは
 円形でコンテンツ枠の外側に絶対配置されるため、next が行の右外へ出てリストの
 `overflow_hidden` で切れ、見えていなかった
 - バーの**ホバーで色が変わる**ようにした（`theme.secondary` はダークで背景の `muted` と
 同色 15% のため変化が見えなかった。`views::hover_bg` のモード別グレーを使用）。ホバーと
 `cursor_pointer` は移動できる向きだけに付ける
 - バーを**移動できない向きは無効表示**にした（アイコン `muted_foreground.opacity(0.5)` +
 背景 `muted.opacity(0.4)` = Button の disabled と同じ薄さ。ホバー応答なし）
 - バーのクリックが**行へ伝播してビューアーが開く**ことがあったのを修正（移動できない向きは
 クリックハンドラを登録していなかったため、クリックが行へ抜けていた）。常にハンドラを
 登録して `stop_propagation` する
 - カルーセルのスナップを**約 2 倍速く**した（テーマの `motion.spring_move` を
 280ms / 減衰 0.85 → 120ms / 0.9 に上書き。静止までの実測 256ms → 136ms）
 - タグ情報エリアを**上詰め**に変更（`content_start()`。既定の `align-content: stretch` では
 タグが無い / 少ない本で編集ボタンが縦中央に落ちていた）
 - リスト表示のタグ列を、**表示エリアの幅から入る個数を計算**して折りたたむようにした
 （個数の固定上限を廃止。「+n」チップのクリックで全件表示 = 列 / 行が伸びる、「閉じる」で
 折りたたみに戻す）。タグ名の幅は GPUI のテキスト計測で実測し、タグ列の幅 × 折り返し 4 行
 （表紙の高さ 133px ぶん）に詰める。実測（「タグ01」形式 20 件のリスト表示）: 窓 700px = 1 件 /
 1000px = 7 件 / 1400px = 11 件 / 1920px = 15 件で、行の高さはタグ無しの行と同じ 150px のまま。
 タグの折り返しで行の高さが伸び、固定サイズの表紙 / カルーセルとの間に大きな余白ができていた
 のを解消（行の高さが全行で揃う）
 - タグを展開して行が高くなったときは、カルーセルも表紙と同じく**上揃え**にした
 （縦中央に浮かない）
 - リスト表示の**表紙とカルーセルのサムネイルを一回り大きく**した（表紙枠 162x108 → 200x133、
 行の高さ 124 → 150）。カルーセルは**列幅を 2 等分して埋める**ようにし、窓幅いっぱいに
 サムネが並ぶ（大きさの上限は表紙と同じ。実測: 1484px 幅で 196x130）。窓幅でサムネが
 伸縮するが、行の高さは展開しない限り表紙の高さで一定
- 本棚カード（グリッド / リスト）に**サークル名・作者名**を表示: `bookshelf_items` に `author` カラムを追加（`migrate()` のプログラム的マイグレーションで冪等に ALTER TABLE）。技術書典は author を持たないため空のまま表示せず、BOOTH 等は作成者名（shop）を author に格納して表示
- 閲覧履歴機能（`view_history` テーブル）: ビューアーを開いたとき開始・閉じたとき終了を記録し、閲覧回数と閲覧時間を取得可能に。右クリックメニューの最上部に閲覧回数を表示
- ページ毎の閲覧記録（`page_views` テーブル）: ビューアーで各ページを表示した回数（`view_count`）と滞在時間（`total_seconds`）を 1 書籍 × 1 ページで累計記録。ページ移動・ビューアーを閉じたときに確定（最終ページの滞在はクローズ時に確定）。見開きは左右両ページを計上。試し読みは対象外。Google Drive バックアップ対象にも含む
- 本棚のキーボード操作: 矢印キー / `hjkl` による選択移動、`Enter` で選択カードを開く。選択タイルのハイライト（背景色 + プライマリ枠線）、選択に応じたスクロール追従
- ビューアー: ダブルクリックでズーム（2 倍 → 6 倍 → 解除）、拡大中のドラッグパン（補間 + 慣性・Google マップ風）、ズーム時のメニュー非表示
- ビューアーのショートカット追加: `↑` / `k` / `backspace` で本棚に戻る、`esc` をメニュー表示に拡張、左綴じ/右綴じで矢印キーの方向を反転
- **付箋（ページ単位のメモ）段階 1**: ビューアのページ画像の上端（右寄せ）に付箋アイコンを追加。
  アイコンはしおり（リボン）型（Lucide `bookmark`、ISC）で、ON / OFF とも同じ形（未付箋は
  半透明の灰色、付箋ありは青系 + 中を半透明の青で塗る）。**上辺の線はアセット側で描いていない**
  （左右の縦線だけ）ので画像の上端に置いても線が出ず、はみ出しも起きない。一辺は
  `NOTE_ICON_SIZE`（28px）で共有し、**実際に描かれている画像の上端・右端**に合わせる。
  **付箋ありのアイコンを押すと解除**でき、そのとき**メモは残る**（`is_active` を分けたので
  付け直すとメモが戻る。誤操作でメモを失わない）。表示・非表示はトップバー / ボトムドックと同じ条件
  （`overlay_visible && !zoomed`）に揃え、`deferred` で最前面に描くので戻る領域 / ページ送り帯の
  上でもクリックでき、付箋を押してもページ送り・戻るは起きない。
- **付箋画面 段階 2**: サイドバーの「付箋」（Lucide `sticky-note`、ISC。閲覧履歴の下）から、
  付箋を**追加が新しい順**に一覧する。行は本棚のリスト形式と同じ 4 列で、サムネイルは
  **付箋を付けたページの画像**（リーダーと同じ縮小経路）、情報に加えて**メモ**と
  **付箋登録日**（ローカル日付）を出し、タグ列は本棚と同じチップ・ハート・折りたたみ。
  カルーセルは出さない。右端の「**本を見る**」（矢印なし・行の高さいっぱいの塗りボタン）で
  ビューアを**そのページ・その見開き側**で開く（`spread_side` があるときは見開きモードにして
  左右を合わせる）。
  「このページに付箋を付けました」ダイアログでメモを入力して OK で保存する。1 ページ 1 件で、
  同じページに付け直すとメモを上書き（既存のメモは開いたときに読み込まれる）。見開きでは
  付けたときの左右（`spread_side`）も保存し、「本を見る」から同じ側に開けるようにする。
  新テーブル `page_notes` はマイグレーションファイルを作らず `CREATE TABLE IF NOT EXISTS` で
  冪等に適用する（アイコンは Lucide `sticky-note`、ISC）。付箋項目画面とサイドバーの導線は段階 2
- サイドバー: 閉じた状態で本棚アイコンをダブルクリックするとサイドバー + 本棚メニューを開く
- **閲覧履歴画面**（サイドバーの「閲覧履歴」/ Lucide `history` アイコン、ISC ライセンス）:
  `view_history` の閲覧セッションを **1 日 1 本**に集約し、`2026年09月13日` の日付見出しの下に
  本棚と同じ見た目のカード / リストで並べる。トップに件数・期間フィルタ（全項目 / 今日 /
  今週 / 今月。今週は月曜始まり）・サイトフィルタ（履歴に含まれるサイト）・表示切替を置く。
  日付は背景色つきの**バー**で出し、1 件の情報は**本棚と同じ**（表紙 + タイトル + イベント名 or
  購入日 + サークル/作者 + 進捗 + 読了/未読 + ♡ + タグ列。閲覧時刻・閲覧時間・種別ラベル・
  タグ編集は出さない）。タグ列は本棚と同じチップ・ハート・「+n」折りたたみ（`render_tag_chips`
  を共用）で、文字クリックの絞り込みも効く。クリックでリーダー、♡ でお気に入りをトグル。
  キーボード操作（矢印 / hjkl / Enter / ESC）と選択ハイライトも本棚と同じ。
  リスト形式は本棚の行と同じ **4 列構成**（表紙 200x133 / 情報列 320px 固定 / タグ列 20% /
  残りは空き）で、サークル・作者は本棚と同じハート付きチップ。表紙は本棚と同じ解決順
  （サイトのサムネイルキャッシュ → pack の表紙 → プレースホルダ）で、カードは本棚と同じ
  角丸・余白色・ダウンロード済みバッジ、行は角丸なし・余白 `muted` の枠を共有する時刻は UTC で保存されているため
  `view_history::list_daily` がローカル時刻に直してから日付でまとめる（新規 API + 単体テスト）。
  本棚と同じ所有者フィルタを通すのでアカウント切替で他アカウントの履歴は出ない
- 本棚カード（グリッド）のタグを **6 件で折りたたみ**、「+n」/「閉じる」で展開できるようにした
  （リストの折りたたみと同じ状態・同じチップ）。カードは行の高さを共有するため、タグの多い
  本が 1 冊あると行全体が間延びしていた（実測: 幅 190px のカードで 20 タグ = 10 行・+295.0px →
  6 件 = 3 行・+90.0px）
- ドキュメント: `docs/features.md`、`docs/database.md`、README、LICENSE（MIT）

- **本棚の並び替え**: 「並び替え」ボタン（表示切替の左隣）で
  購入日 / 発売日 / タイトル / 最終閲覧日 / 閲覧回数 / 閲覧時間 / サイズ の
  **7 項目 × 昇順・降順**。日付はサイトごとの形式差（RFC3339 / `YYYY/MM/DD` /
  `YYYY年MM月DD日`）を正規化して比較し、**データが無い項目はメニューに出さない**
  （発売日 = DLsite のみ、サイズ = ローカルのみ）
- **通知（gpui-kit の Notification）**: メッセージをウィンドウ右上のトーストで出す
  （Info / Success / Error の 3 種。既定 5 秒で自動的に消える）。自前の下部バーと
  赤い 1 行は廃止した
- **付箋画面の検索**: 「全項目」ボタンの隣の検索欄で、メモ・タイトル・サークル名・
  著者名のキーワード絞り込み（1 文字入力ごとに反映）
- **関連書籍カルーセル**: 未ダウンロード本をクリックすると
  「未ダウンロードです。ダウンロードしますか？」の確認ダイアログを出し、
  「はい」で取り込み → **完了後にビューアーを開く**
- **閲覧統計**: `view_history::view_stats`（1 クエリで全書籍の閲覧回数・累計閲覧時間・
  最終閲覧を集計）を追加し、並び替えのソートキーに使う

### Changed

- **読書状態を 未読 / 読書中 / 読了 の 3 状態に統一**（判定は core の
  `ReadingState::from_progress` 1 か所）: 本棚のカードバッジ・リスト行の状態チップ
  （読書中は青）・フィルタ（**未読 / 読書中 / 既読** の 3 択に拡張）・履歴画面・設定の
  冊数集計が同じ判定を通る。途中まで読んだ本は今まで「未読」に丸められていた。
  あわせて設定の集計が `current_page + 1 >= total_pages` で **1 ページ早く読了**に
  なっていたのを修正（デスクトップの進捗は 1-indexed）
- `gpui-kit` の依存元を crates.io（0.6.1）から上流リポジトリの main（rev 固定
  `84f57fdfcb4910623fb0bb7f795b077e249f9271`）へ変更。**`Carousel`（Multiple Item）が
  未リリース**（0.6.1 の `gpui-component` に carousel モジュールが無い）ため、
  関連書籍カルーセルで使うのに必要。Carousel を含む版が crates.io に出たら差し替える
- リーダーの画像読み込みを高速化:
  - webp デコードを libwebp（直接 RGBA）に切り替え（image クレート比で数十 ms に短縮）
  - .opfspack のバイト列を OnceLock で 1 回だけ読み込み
  - 暗号化 pack のキー導出（PBKDF2）を 1 回だけ実行（`derived_pack_key` + `read_entry_with_key`）
  - 現在ページ ±2 のプリロード、デコード済みページを直近 30 ページ保持、二重ロード防止
  - 「読込中…」テキストを廃止してスピナー表示に
- 本棚のグリッドを仮想化リストに変更（スクロールの高速化）とフィルタキャッシュ
- **本棚のリスト表示も仮想化**（可視行のみ構築）。グリッドと同じ `ListState` を行に使い、
  全行の表紙・タグ計測・カルーセルを毎フレーム構築していたのを解消（スクロールの引っかかり）。
  行は仮想化リストが `max-content` で測るため、幅を `w_full()` で明示してタグ列（行幅の 20%）が
  内容なりに広がらないようにした（広がるとタグが折り返さず行が伸びない）。描画ごとに走っていた
  `visible_shelf_cards`（全カードのソート）は `filtered` キャッシュで代替
- 読書進捗を 1-indexed に統一（`reading_progress.current_page`）
- 表紙キャッシュを縮小 PNG（幅 288px）で保存し、reload を高速化
- イベント名補完の DB 書き込みを廃止（メモリ上で補完）
- 本棚のタグの並びを **選択中 → お気に入り → 集計数（そのタグを持つカード数）の多い順 →
  名前順** に変更。選択中 / お気に入りはチップを押した瞬間に並び替わり（reload なし）、集計は
  reload 時に更新する。カード / リスト / お気に入りタグ一覧で同じ並びになる（追加の SQL は
  無し。実測: 集計 1000 冊 × 30 タグで 33.7ms、1 フレーム分の並び替え 25 冊 × 30 タグで
  1.06ms・debug）。**折りたたみ中に選択したタグが隠れない**ようになり、解除すると元の位置に戻る

- メッセージ表示を **gpui-kit の Notification に統一**（`app_state::ToastKind` →
  `NotificationType`）。進捗・案内 = Info、完了 = Success、失敗 = Error。
  `BookshelfView` の `toast` / `error` フィールドと描画は削除した
- **スクロールモードのページ保持にバイト予算**（`SCROLL_CACHE_BUDGET_BYTES` = 384MiB）を導入。
  表示中ページの前後だけを保持し、外れたページは CPU（`Arc<RenderImage>`）と
  GPU（`window.drop_image`）の両方から解放する。1 冊のピークは最悪 5.6GB → 約 0.4GB
- 付箋・チェックリストのサムネイルを**上限つき LRU（64）**にし、あふれた分は
  `window.drop_image` で解放する（付箋は描画時に未キャッシュの行だけ背景デコード）
- 履歴画面の表紙を **1 冊 1 回だけデコードして共有**（同じ本が複数の日に出るため。
  実測 38 要素 → 22 冊ぶん ≒ 24MiB）
- **本棚の下部 28px の余白を削除**（下部パディング + 非表示ダイアログぶんの gap。
  1 行でも多く本を表示するため）

### Fixed
- **Google のトークンが失効したときの復帰**: リフレッシュトークンが取り消し・失効している
  （`invalid_grant`）と、Drive 同期が `token endpoint status 400: {"error":"invalid_grant"…}` の
  ような**生の応答をそのまま画面に出して**、以降も同じ失敗を繰り返していた。失効を型
  （`GoogleError::RefreshTokenRevoked`）として扱い、画面には日本語の案内
  （「Google のログインが無効になりました…もう一度ログインしてください」）を出し、保存済みの
  失効トークン（メモリ + keyring）を破棄して Google ログインの導線を自動で開くようにした。
  なお、この導線は認証モーダルを `Workspace::open_auth` 経由で開く（`dispatch_action` は
  WebView 作成時にウィンドウの RefCell を再入して**アプリが固まる**ため。既存の
  「identity required」の導線も同じ経路に直した）
- 仕様書の作成中に見つかった不整合を修正: **並び替えの永続化**（`bookshelf.sort_field` /
  `bookshelf.sort_ascending` に保存し起動時に復元）/ **タグ絞り込みが未ダウンロード本に効かない**
  （本棚アイテムの `tags_json` も対象に）/ **見開きの滞在時間が 2 倍に計上**（左右へ均等配分）/
  **単一・見開きモードの追い出しで GPU テクスチャが残る**（`drop_image` を追加）/
  **`view_history::touch` が未使用**（削除してテスト専用ヘルパへ）/
  **`reading_progress.scroll_position` が常に 0**（Rust 側のフィールドを削除）
- Drive の保存先フォルダがアカウント別でない点を `docs/account-switch.md` に明記（実装は単一キー）
- 古いテスト（`migrate_creates_all_schema_tables`）の期待表に `page_notes` を追加


- ビューアーを閉じた後にビジー状態になる問題（全件 reload → 該当カードのみ更新）
- 未読の本が最終ページから開く問題（初期ページのアンダーフロー対策）
- ページスライダーが動かない問題（ページ移動用スライダーを追加）
- 見開きで片方のページだけ先に表示される問題（アトミック表示 / 1 枚の画像として拡大）
- 2 重起動（同一アプリの同時起動）を防止: config ディレクトリのロックファイルを OS の
  ファイルロックで排他し、2 個目の起動はログを残して終了する（多重起動で DB・パック・
  ダウンロード先を共有し、二重取り込みや OAuth ポートの取り合いが起きていた）

- **PDF の並列取り込みで稀にクラッシュする問題**: PDFium は同時利用がスレッドセーフでなく
  （テキスト入り PDF を 8 スレッドで同時描画すると `STATUS_ACCESS_VIOLATION`）、
  PDFium を使う区間を Mutex で直列化した
- **メモリ使用量 6,000MB**: ビューアー（`self_handle` の自己強参照）と `ReaderView`
  （グローバル action listener の強参照）が解放されず、開いた本ごとに累積していたのを
  `WeakEntity` 化。スクロールモードの全ページ保持も上記の予算で解消
- **表紙のプレースホルダが常に空枠だった問題**: SVG を `image` クレートで復号しようとして
  常に `None` になっていた。usvg/resvg でラスタライズするように修正
- ログイン画面（BOOTH / FANZA / DLsite / 技術書典 / Google）の URL 監視タスクが
  WebView ごと残っていたのを `WeakEntity` 化
- 履歴テストが**実行時刻（ローカル深夜）で落ちる**問題を、日付に依存しない形に修正

## [0.0.1] - 2026-08-27

### Added

- 初期リリース: Web 版の機能移植（本棚・リーダー・チェックリスト・設定）
- 技術書典/BOOTH 同期、Google Drive 同期、Zenn タグ
- .opfspack フォーマット（Rust 移植）による画像アーカイブ
